from setuptools import setup, find_packages

setup(
    name="viz_marker_publisher",
    version="0.1.0",
    description="A ROS utility for publishing markers",
    packages=find_packages(),
    install_requires=[],
)
